import { useJobsStore } from '../store/jobs-store';
export function JobsList() { const { jobs, activeId, selectJob } = useJobsStore(); return <section className="card"><h2>Задания</h2>{jobs.length === 0 ? <p className="muted">Заданий пока нет.</p> : <ul className="jobs">{jobs.map((job) => <li key={job.id}><button className={activeId === job.id ? 'job active' : 'job'} onClick={() => void selectJob(job.id)}><span><b>{job.status}</b> · {new Date(job.createdAt).toLocaleString()}</span><small>{job.id.slice(0, 8)} · {job.stats.success} OK / {job.stats.error} ошибок · {job.urlCount} URL</small></button></li>)}</ul>}</section>; }
